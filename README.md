# Keycloak Agent

Keycloak Identity MCP Server and Agent for Agentic AI!

## Environment Variables
- `KEYCLOAK_URL`: The base service URL.
- `KEYCLOAK_TOKEN`: The API / bearer token for authentication.

## CLI Usage
```bash
python -m keycloak_agent --help
```
